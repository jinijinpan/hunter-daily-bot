# 里程碑 5 真实环境验收记录

## 当前进度

- 验收日期：2026-07-30
- 角色：`帝临｜微笑`
- 单角色连续成功：`1/3`
- 剩余单角色成功次数：`2`
- 三角色验收：未启用、未开始
- 天梯赛：始终排除；未执行天梯赛任务，未领取天梯赛奖励

## 第一次单角色成功

本次验收按同一角色的连续修复与续跑链统计。权威起始运行目录为
`runs/20260730-072032`，小秘书首次预扫描为 `0/7`。最终成功运行目录为
`runs/20260730-130136`；该进程从同一角色 `6/7` 的现场状态开始，实际完成最后一项
无限秘境后，两次扫描均确认 `7/7`。因此本次成功不是用启动时已经显示 `7/7` 的
角色充数。

- 开始状态：主界面，`0/7`
- 结束状态：小秘书任务页，预扫描 `7/7`，结束复核 `7/7`
- 最终进程退出码：`0`
- 最终日志：无 `ERROR`，无安全停止，无角色切换
- 猎人联赛职级复核：`10/10`

## 七项任务证据

| 任务 | 实际执行 | 完成复核 |
| --- | --- | --- |
| 无尽塔 | `runs/20260730-072032` 进入塔、开始并确认战斗、处理战斗结算 | `runs/20260730-074348` 任务扫描 `tower=True`；最终目录再次为 `True` |
| 猎魔战场 | `runs/20260730-072032` 执行挑战/速通链路 | 同目录记录“猎魔战场完成验证通过”；最终目录再次为 `True` |
| 资源补给 | `runs/20260730-072032` 执行快速通关、确认、关闭奖励和返回 | `runs/20260730-074348` 扫描 `resource_supply=True`；最终目录再次为 `True` |
| 深渊挑战 | `runs/20260730-072032` 完成第 1 次挑战并处理结算 | `runs/20260730-074348` 扫描 `abyss=True`；最终目录再次为 `True` |
| 魔物入侵 | `runs/20260730-074348` 记录第 1 至第 4 次实际完成 | 同目录扫描 `monster_invasion=True`；最终目录再次为 `True` |
| 猎人联赛 | `runs/20260730-075209` 从 `0/10` 自动完成第 1 至第 10 场 | 同目录职级任务复核 `10/10`；`runs/20260730-130136` 再次确认 `10/10` |
| 无限秘境 | `runs/20260730-130136` 实际完成三关，三次评分后返回安全区 | 同目录两次任务扫描均为 `infinite_mystery=True`，结束复核 `7/7` |

## 失败与修复重试

以下失败均安全停止；诊断后修复并在同一未完成角色上续跑：

| 运行目录 | 停止原因 | 后续修复 |
| --- | --- | --- |
| `runs/20260730-072032` | 深渊奖励过渡帧被重复使用旧坐标，达到 5 次上限 | 过渡帧等待稳定，不再重试陈旧目标 |
| `runs/20260730-073819` | 魔物结算后直接回到关卡页，被当作等待 `monster_result` 超时 | 接受直接返回 `monster_stage` |
| `runs/20260730-074348` | 猎人联赛分数变化奖励层后等待职级任务页超时 | 补充联赛奖励层恢复链路 |
| `runs/20260730-075209` | 段位下降页面未识别，等待 `rank_overlay` 超时 | 支持“段位下降”标题及关闭控件 |
| `runs/20260730-081138` | 微信小游戏窗口未打开 | 用户重新打开并保持窗口稳定 |
| `runs/20260730-123503` | 返回主页有延迟，已经到主页后仍按旧目标等待任务页 | 动作验证接受延迟到达的目标状态 |
| `runs/20260730-123906` | Windows 拒绝前台激活 | 增加不移动、不缩放窗口的临时置顶聚焦回退 |
| `runs/20260730-124214` | 职级首页过渡帧未检测到任务标签 | 等待稳定帧后重新检测 |
| `runs/20260730-124652` | 稳定帧 OCR 仍漏检职级任务标签 | 增加任务标签模板回退 |
| `runs/20260730-125030` | 联赛 `10/10` 复核后再次发生前台激活失败 | 完成并验证窗口聚焦回退 |
| `runs/20260730-125359` | 段位下降关闭成功，但宽布局无限秘境大厅未识别 | 增加宽布局真实样本和页面搜索边距 |

最终成功目录 `runs/20260730-130136` 未发生失败重试、安全停止或错误。日志中的
警告均为有限的稳定采样或安全恢复路由，不包含未知页面猜测点击。

## 奖励面板恢复

`runs/20260729-234438` 的遗留奖励面板已加入最小真实回放。V2 使用“奖励”、
“每日奖励”、“结算奖励”和右上角关闭控件的组合判定 `ladder_reward_panel`，只暴露
`close_reward_panel`。面板内“领取”与说明文字不会成为 `dismiss_reward`，启动恢复也
只点击右上角 X；天梯赛奖励不会被领取。

## 离线验证

2026-07-31 按要求顺序复核，完整测试只运行一次：

| 验证 | 结果 | 框架耗时 | 墙钟耗时 |
| --- | --- | --- | --- |
| `python run_tests.py --profile fast` | `140 passed` | `4.639s` | `5.8s` |
| `python run_tests.py --profile full` | `156 passed` | `218.222s` | `219.4s` |
| `python replay_recognition.py` | `31/31 samples, exit 0` | - | `211.5s` |

首次 fast 复核发现三角色功能测试隐式依赖全局 `character_cycle_count=3`。当前验收配置
固定为 `1` 后该用例失败；测试现已自行显式设置 `3`，既保留三角色功能覆盖，也不会重新
开启三角色验收。修复后 fast 通过，随后执行了唯一一次 full 和一次完整回放。

## 后续验收

还需完成同等标准的单角色成功运行 `2` 次，才可进入三角色验收。当前仍使用
`character_cycle_count=1` 的临时验收配置，三角色自动循环保持关闭。
